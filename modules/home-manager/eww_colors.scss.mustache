// Base16 color definitions
$base00: {{base00}};
$base01: {{base01}};
$base04: {{base04}};
$base05: {{base05}};
$base08: {{base08}}; // Red
$base09: {{base09}}; // Orange
$base0A: {{base0A}}; // Yellow
$base0B: {{base0B}}; // Green
$base0C: {{base0C}}; // Cyan
$base0D: {{base0D}}; // Blue
$base0E: {{base0E}}; // Purple
$base0F: {{base0F}}; // Brown

// Workspace color definitions
$workspace-background: $base00;
$workspace-inactive: $base01;
$workspace-active: $base0D;    // Using Blue for active
$workspace-occupied: $base09;  // Using Orange for occupied

// Text color definitions
$text-inactive: $base04;
$text-active: $base00;         // Using background color for contrast
$text-occupied: $base00;       // Using background color for contrast

// You can add more custom color variables here if needed
